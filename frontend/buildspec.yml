version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 20
    commands:
      - npm install -g npm@9
      - cd frontend
      - npm ci

  build:
    commands:
      - npm run build

artifacts:
  base-directory: frontend/build
  files:
    - '**/*'
