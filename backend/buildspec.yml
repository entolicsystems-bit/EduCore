version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 20
    commands:
      - cd backend
      - npm ci
      - npx prisma generate

  build:
    commands:
      - npm run build

artifacts:
  files:
    - '**/*'
