version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - cd backend
      - npm ci

  build:
    commands:
      - npx prisma generate
      - npm run build

artifacts:
  files:
    - '**/*'
