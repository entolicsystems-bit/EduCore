version: 0.0
os: linux

files:
  - source: backend
    destination: /home/ec2-user/educore-backend

hooks:
  AfterDeploy:
    - location: backend/backend-deploy.sh
      timeout: 300
      runas: ec2-user
